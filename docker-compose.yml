version: "3.9"
services:
  redis:
    image: redis:7.2
    ports:
      - "6379:6379"
  app:
    build: .
    environment:
      - REDIS_URL=redis://redis:6379
    ports:
      - "8000:8000"
    depends_on:
      - redis
